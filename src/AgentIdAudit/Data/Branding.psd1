# Shown in the footer of every HTML report. ContactLine is optional plain text (HTML-encoded on output);
# leave it empty to show only the product line.
@{
    ProductName = 'AgentIdAudit'
    ProjectUrl  = 'https://github.com/mrochon/AgentIdAudit'
    ContactLine = ''
}
